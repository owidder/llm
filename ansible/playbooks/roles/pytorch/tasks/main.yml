---
- name: 'Clone pytorch repo'
  ansible.builtin.git:
    repo: 'git@github.com:pytorch/pytorch.git'
    dest: '~/pytorch'

- name: 'Create conda environment'
  shell: 'conda create -y --name build-pytorch'

- name: 'Activate conda environment'
  shell: 'conda activate build-pytorch'

- name: 'Install mkl'
  shell: 'conda install mkl mkl-include'

- name: 'Install cuda'
  shell: 'conda install -c pytorch magma-cuda110'
